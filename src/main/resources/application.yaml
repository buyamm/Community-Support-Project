server:
  port: 8080

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true
  datasource:
     url: "jdbc:mysql://${DATABASE_HOST:localhost}:${DATABSE_PORT:3306}/${DATABASE_NAME:community-support}?createDatabaseIfNotExist=true"
      username: ${DATABASE_USER:root}
      password: ${DATABASE_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

cloudinary:
  api-key: 623981926764984
  api-secret: 4KiPpbpjGp_P_9ivae2YkHUxVuM
  cloud-name: dryf5c9eh